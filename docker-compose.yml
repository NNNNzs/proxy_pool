version: '2'
services:
  proxy_pool:
    image: nnnnzs/proxy_pool
    container_name: proxy_pool
    network_mode: "host"
    ports:
      - "5010:5010"
    environment:
      DB_CONN: "redis://127.0.0.1:6377/0"